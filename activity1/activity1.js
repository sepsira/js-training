/*
your goal is to code this such that when a comment is submitted, the text
entered shows up on the right hand side.

consider looking at the documentation for getting and setting values, and
event listeners!

(hint)
document.getElementById("") is something we use a LOT, get used to it
*/

// this function will run itself when your code runs
(function() {
    // write here!
}) ();
